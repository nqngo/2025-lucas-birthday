{
  "name": "rsvp-dino-spa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "deploy-custom": "npm run build && echo 'lucas.ngo.cx' > dist/CNAME && gh-pages -d dist -r https://github.com/yourusername/yourrepo.git"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.3.5"
  }
}
